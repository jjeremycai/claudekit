[project]
name = "ashby-mcp"
version = "0.1.0"
description = "Ashby MCP Server for Claude Code"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
